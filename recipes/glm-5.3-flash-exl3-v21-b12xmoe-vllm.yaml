# Recipe: GLM-5.3-Flash EXL3 v21-b12xmoe (TP=2 across 2x DGX Spark, vLLM native multi-node)
#
# *** v21-b12xmoe PROMOTED (2026-09-19): identical to v20-upstreamsync
# below except the container tag and the routed-expert MoE path, now the
# b12x fused-trellis kernel (GLM53_EXL3_B12X_MOE=1) instead of this fork's
# original per-expert/turboderp-exllamav3_ext path. Validated across
# three separate GPU passes today (memory/zero-copy, correctness parity,
# throughput) plus a direct real-traffic measurement of the one known
# limitation (the SwiGLU clamp gap -- real but rare, accepted). See the
# top-level `description:` field above and recipes/VALIDATION.md for full
# context. Everything below this point is v20-upstreamsync's own header,
# unchanged, since every fix it documents still applies identically here.
#
# *** CANDIDATE, NOT YET PROMOTED (2026-09-11) — v19-indexercompat remains
# production while this is validated. Sourced from a routine upstream-check
# triage across MiaAI-Lab's GLM-5.3-Flash-EXL3-2x-DGX-Sparks fork
# (9c0794b..1caea9a) and Enntity/sparkglm's exl3 branch. Seeded from
# v19-indexercompat unchanged, PLUS four items: (1) files/chat_template.jinja
# -- fixed a live bug where an assistant turn with content=None
# (tool-calls-only, common in agent traffic) rendered the literal string
# "None" into the prompt (upstream MiaAI-Lab#156, is-not-none guard only);
# (2) start.sh count_shards() -- fixed a false-completeness bug where stale
# shards in an old cached snapshot could satisfy the weight-present check
# even though the ACTIVE snapshot is incomplete (upstream MiaAI-Lab#153),
# same fix applied to this fork's own separate DFlash2 shard-count call site;
# (3) LONG_PREFILL_TOKEN_THRESHOLD -- new opt-in vLLM scheduler flag
# plumbing (upstream MiaAI-Lab#157), left EMPTY/off by default -- this fork
# already answers the same "long prefill freezes a warm session" symptom via
# its own overlay/patch_scheduler_decode_floor.py, untested interaction;
# (4) BUILD_MIN_MEM_GIB -- a build-host guard refusing `docker build` under
# <32 GiB MemAvailable (ported from Enntity/sparkglm a8aaa229), build-host
# side only, does not touch the serving path. vLLM PR #54929 (a candidate
# fix for this fleet's recurring #51921 shm-broadcast stall) was assessed
# and NOT vendored -- unreviewed, has known open bugs, targets a backend
# this fork doesn't use, contested root-cause match to #51921. Full detail:
# recipes/build/glm53-exl3-v20-upstreamsync/NOTES.md.
#
# Docker build PASSED (63/63 steps incl. full build-time self-test suite).
# tinyGLM gate PASSED (one transient CUDA-graph-capture crash on first
# attempt, unrelated to any of the four items above -- none touch that code
# path; clean retry matched v19-indexercompat's dispatch-correctness
# confirmation lines exactly, determinism confirmed). This recipe is the
# real-checkpoint validation step -- same model/flags as v19-indexercompat,
# only the container tag changes, so any behavioral difference found here is
# a genuine effect of one of the four items (most likely candidates: item 1,
# if a tool-calling test happens to exercise a null-content turn) or a
# regression, not expected drift.
#
# --- below this point retains v19-indexercompat's original header verbatim,
# since every fix it documents is unchanged in this candidate ---
#
# *** v19-indexercompat, 2026-09-09 header (unchanged, retained for
# provenance) ***
#
# *** CANDIDATE, NOT YET PROMOTED (2026-09-09) — v15-combined remains
# production while this is validated. Candidate 3 of 3 (LAST) in this
# downtime window (candidate 1, v17-nopemha, NOT promoted -- dead code;
# candidate 2, v18-gb10gemm, PASSED, recommended for promotion). Seeded from
# v18-gb10gemm, NOT v15-combined directly, since #54048 is going forward.
#
# Different in kind from the first two candidates: NOT new upstream
# functionality. This hardens our OWN already-shipped
# overlay/patch_indexer_workspace.py against a known future landmine:
# upstream vLLM PR #51718 ("[6/N][KV-Cache Layout Refactor] Standardize KV
# cache layout", merged into the vLLM lineage feeding v0.29.0, NOT present
# in our current base image -- this build does not rebase onto it) renames
# vllm/v1/attention/backends/mla/indexer.py's
# MLAAttentionSpec.compress_ratio -> .tokens_per_state (adding an
# isinstance(..., int) assert) and .storage_block_size -> .num_states. Our
# patch pins an anchor on the literal current-base `compress_ratio` line
# inside DeepseekV32IndexerMetadataBuilder.__init__ -- already fail-closed
# by design (raises a clear error on anchor drift rather than silently
# no-op'ing or corrupting), so this was not an active bug, it was a known
# future build-time failure whenever this fork eventually rebases onto a
# vLLM base carrying #51718.
#
# Fix: the patch's anchor-matching now tries the current-base form first,
# falls back to the post-#51718 form if the old one isn't found, and only
# raises fail-closed if NEITHER matches -- now naming both forms tried. The
# `storage_block_size`/`num_states` rename needs no equivalent treatment:
# neither appears anywhere in this patch script (confirmed by grep) --
# those two call sites live in build(), which this patch never touches.
# `ANCHOR_SIZE`/get_max_prefill_buffer_size is also untouched by #51718
# (confirmed directly against the PR's own diff, not just a prior summary).
#
# Old-anchor form LIVE-VERIFIED (2026-09-09) against a throwaway container
# of glm53-exl3-v18-gb10gemm:local: still compress_ratio/storage_block_size,
# zero tokens_per_state/num_states. New-anchor form is NOT live-verified --
# no v0.29.0-era container available to check against -- transcribed
# directly from #51718's own diff instead. Zero functional change expected
# on this build's actual base: the old anchor still matches, so the patch
# applies byte-for-byte identically to v18-gb10gemm's, proved directly by
# test_current_base_patch_is_byte_identical_to_manual_replacement in the
# extended tests/test_indexer_workspace.py (29 tests total, all host-only,
# no GPU). See recipes/build/glm53-exl3-v19-indexercompat/NOTES.md for the
# full trace.
#
# This is the LAST candidate in this downtime window -- production still
# needs to come back up once this validates.
#
# --- everything below this point retains v18-gb10gemm's header verbatim,
# since every fix it documents is unchanged in this candidate ---
#
# *** CANDIDATE, NOT YET PROMOTED (2026-09-09) — v15-combined remains
# production while this is validated. Candidate 2 of 3 in this downtime
# window (candidate 1, v17-nopemha, was NOT promoted -- dead code, blocked
# by our own already-shipped fix for a different upstream PR, #51395).
# Seeded from v15-combined + vllm-project/vllm#54048 ("[Bugfix][MoE] Enable
# cuBLAS out_dtype router GEMM on all CUDA archs (fixes family-120/GB10)").
# Zero anchor drift from upstream's own diff context -- see
# recipes/build/glm53-exl3-v18-gb10gemm/NOTES.md for the full trace and the
# distinction from this build's already-present MoE gate-dedup patch
# (different file, different bug: dedup controls call COUNT, this patch
# controls GEMM TIER). Precision/correctness fix (fp32-accumulated router
# logits instead of bf16-rounded-then-cast), not primarily a throughput
# play. ***
#
# --- everything below this point retains v15-combined's original header
# verbatim, since every fix it documents is unchanged in this candidate ---
#
# *** VALIDATED AND PROMOTED TO DEFAULT 2026-09-08 (supersedes v12-combined,
# same day) — full results in VALIDATION.md "What's-next goal: FlashKDA,
# MoE gate dedup, and the 240K memory-pressure investigation concluded".
# v12-combined (glm-5.3-flash-exl3-v12-combined-vllm.yaml) and v9-fatfork
# are both retained as known-good fallback targets. ***
#
# WHAT'S NEW OVER v12-combined:
#   1. vLLM PR #55737 (FlashKDA): fused CUDA kernel replacing the Triton
#      KDA chunked-prefill path (GLM53_FLASHKDA_PREFILL=1, fail-closed).
#      Measured on this fleet: ~1650 tok/s @16K (vs. E3-alone's ~1557),
#      ~1662 tok/s @64K (vs. ~1623) -- a real, modest improvement, not
#      independently pushed to 128K/240K due to memory-margin caution on
#      the day of testing. Real bug caught and fixed during integration:
#      our ported call passed 2 more args than this image's compiled
#      _flashkda_C.fwd() accepts (16 vs. its real 14) -- caught by tinyGLM
#      before ever reaching a real-checkpoint boot.
#   2. vLLM PR #55736 commit 3/3 (MoE router-GEMM dedup): removes a
#      confirmed duplicate gate/router GEMM (MoERunner already recomputes
#      it internally regardless of what's passed in -- traced directly
#      against this image's installed MoERunner._forward_impl). Correctness
#      confirmed, throughput statistically indistinguishable from the
#      pre-patch baseline (~1575 tok/s @240K both before and after) --
#      matches the computed FLOP-level ceiling (~0.58% of per-layer MoE
#      compute), adopted on correctness grounds, not as a throughput win.
#
# The 240K single-request memory-pressure investigation (armed the
# OOM-observer diagnostic, deliberately reproduced the scenario): the real
# NVRM out-of-memory kernel events reproduced again, a 5th time across every
# kernel/patch combination tested this project -- and the OOM-observer's
# trace directory was EMPTY afterward. PyTorch's own allocator stats at the
# moment of failure showed 5.8 GiB of free headroom in its own pool -- the
# failure is happening BELOW PyTorch's allocator layer entirely (a driver/
# host-level memory-descriptor allocation failure, not a normal CUDA OOM),
# which is why this specific diagnostic mechanism can't catch it. Confirmed,
# not just theorized, kernel-independent and patch-independent. Still an
# open investigation track -- see VALIDATION.md.
#
# --- everything below this point retains v12-combined's original header
# verbatim for its own WHAT'S NEW OVER v9-fatfork section ---
#
# WHAT'S NEW OVER v9-fatfork:
#   1. MiaAI-Lab's own E3 grouped-MoE kernel (EXL3_FAT_GROUPED=1), grafted
#      from their upstream and tier-reconciled with sparkglm's kernel
#      (already shipped since v9-fatfork, EXL3_GROUPED_PREFILL_K4=1 -- both
#      stay set): E3 is primary, sparkglm's kernel is the real fallback
#      chain, not a second racing path. Validated at real context sizes up
#      to our 262,144 max_model_len ceiling -- no cliff, ~1,557-1,623 tok/s
#      effective prefill across 16K-240K, comparable to or modestly better
#      than v9-fatfork's own validated ~1,510-1,524 tok/s at 16K. This is
#      the first-ever measurement of an E3-class kernel at this scale --
#      MiaAI-Lab's own benchmarks stop at 100K, 256K/300K explicitly
#      unmeasured by them.
#   2. One vLLM upstream PR backport: #55736 commit 2/3 (NoPE MQA
#      write-path fix), unconditional, verified byte-for-byte against this
#      image's installed source. Four other PR items investigated and
#      deferred for real version-drift reasons (target modules/functions
#      genuinely don't match this image's installed vLLM) -- see
#      recipes/build/glm53-exl3-v10-vllmpatch/NOTES.md.
#   3. An inert-by-default TORCH_MEM_FRACTION / CUDA-allocator OOM-observer
#      diagnostic (ported from mmastrac/glm-5.3-flash-4x-gx10), aimed at
#      this project's long-unsolved "silent kill" crash signature. Not
#      armed by default -- zero behavior change unless TORCH_MEM_FRACTION/
#      TORCH_MEM_TRACE_DIR/TORCH_MEM_STATS_DIR are explicitly set.
#
# IMPORTANT FINDING FROM PROMOTION VALIDATION: a real, reproducible memory-
# pressure risk exists at ~240K-token SINGLE-REQUEST prefill on this fleet
# at gpu_memory_utilization=0.84 -- real NVRM "Out of memory [NV_ERR_NO_
# MEMORY]" kernel-log events on both nodes, margin dropping to ~2.5% mem-
# available (uncomfortably close to earlyoom's 2%/80% simultaneous trigger).
# CONFIRMED KERNEL-INDEPENDENT via a controlled A/B (identical test with
# EXL3_FAT_GROUPED=0): the same NVRM events reproduced at the same point,
# comparable magnitude -- this risk already existed in production before
# this promotion (v9-fatfork shares the same sparkglm kernel, same GMU,
# same general memory budget) and is not something E3 introduces. Neither
# config actually crashed in testing. Flagged as a new, not-yet-started
# investigation track requiring human review before further autonomous
# work -- see VALIDATION.md and the v12_combined_promoted memory note.
#
# gpu_memory_utilization deliberately left at 0.84 -- did NOT inherit E3's
# own upstream GPU_MEM_UTIL=0.85 default, which this fleet has already
# proven crashes under load (see the gmu_086_earlyoom_regression history
# below and VALIDATION.md).
#
# --- everything below this point retains v9-fatfork's original header
# verbatim, since every fix it documents remains unchanged in this
# candidate ---
#
# WHAT'S NEW: everything v8 already has (FP8 LM head, K-pool tail fix,
# indexer workspace rightsizing, mamba race-condition backport, mixed-
# prefill env-propagation fix, TileLang JIT-cache persistence), PLUS a
# from-source graft of Enntity/sparkglm's GPU-resident grouped-prefill
# fat-expert kernel (EXL3_GROUPED_PREFILL_K4). This is the first recipe in
# this project's history that runs a custom-built image
# (`glm53-exl3-fatfork:local`, built from recipes/build/glm53-exl3-fatfork/)
# rather than the stock `glm53-exl3-v2:c190db1` — every prior version was a
# runtime monkeypatch over that same prebuilt image.
#
# The grafted kernel replaces the per-expert, host-driven fat-expert GEMM
# loop (one `exl3_fat_gemm_kernel` launch per over-128-row expert) with a
# GPU-resident task planner plus 4 phase-wide launches that cover every fat
# expert in one pass, regardless of count. Scope is deliberately grouped-
# prefill only: sparkglm's separate decode-side cooperative kernel
# (EXL3_DECODE_COOP_K4) is excluded -- their own docs flag it as weaker
# evidence, and it is cleanly separable in both the CUDA source and the
# Python dispatch layer. See recipes/build/glm53-exl3-fatfork/Dockerfile and
# overlay/exl3.py's header comments for the full graft boundary and
# provenance chain.
#
# sparkglm's own measured result (their controlled A/B, bit-exact output,
# ~33K actual tokens, DFlash2 k=7): TTFT 25.7s->23.5s (-8.8%), effective
# prefill 1,282->1,405 tok/s (+9.6%). We run MTP-2, not DFlash2 -- this
# project's own mixed-prefill investigation already found MTP-vs-DFlash2
# bookkeeping differences change a scheduler-adjacent result's outcome, so
# their number is a lead to re-measure here, not one we inherit.
#
# --- everything below this point retains v8's original header verbatim,
# since every fix it documents (mamba race, K-pool tail, indexer workspace,
# mixed-prefill env, FP8 lm_head, TileLang cache) is unchanged in this
# candidate ---
#
# WHAT'S NEW (v8, historical): backports a genuine upstream root-cause fix
# for a Mamba state-copy race condition under speculative decoding
# (vllm-project/vllm#50729, merged into vLLM main 2026-08-17) that this
# project's own base image does NOT yet contain.
#
# Context: this project does not build vLLM from source. The production
# image (glm53-exl3-v2:c190db1, from MiaAI-Lab/GLM-5.3-Flash-EXL3-2x-DGX-
# Sparks) is built FROM a pre-built, digest-pinned base image,
# `vllm/vllm-openai:glm53-flash-arm64-cu130@sha256:905c02933be6...`,
# published by an automated `vllmbot` CI pipeline (presumably building
# vLLM's own GLM-5.3-Flash support branch, vllm-project/vllm#53906), with
# ~15 additional source-patch layers applied on top at Docker build time.
# Checked Docker Hub directly (2026-09-03): that tag currently resolves to
# exactly one digest -- the SAME one we already have pinned, last pushed
# 2026-08-26 -- meaning there is no newer published base image to pull,
# and the branch it's built from evidently is NOT kept in continuous sync
# with vLLM main (the base image's most recent push postdates #50729's
# 2026-08-17 merge by 9 days, yet the fix is absent -- confirmed by direct
# diff comparison against this exact container's installed
# vllm/v1/worker/mamba_utils.py, which matches the PR's own "before"
# lines byte-for-byte, including a since-deleted comment). A literal git
# rebase isn't available either: no local/accessible source tree backs
# this base image for us to rebase -- it's a closed, externally-built
# artifact. Given that, the practical way to "get in sync" for a specific,
# understood, already-merged-upstream fix is the same live-monkeypatch
# technique this recipe already uses for the v5 and v7 fixes, applied to
# this fix's three hunks.
#
# Separately, while investigating this: MiaAI-Lab's build already bakes in
# its own fix for the SAME bug class as our v5 K-pool tail-cache patch
# (`overlay/patch_kpool_tail_slotmap.py`, credited to the same original
# reporter, vcruz305) -- but via a narrower defensive clamp
# (`block_indices = tl.minimum(block_indices, block_table_stride - 1)` in
# the paged slot-mapping kernel) rather than our v5 fix's root-cause repair
# (passing `positions=` through `prepare_attn` so the tail builder computes
# correct indices to begin with, never needing a clamp). A clamp silently
# redirects an out-of-bounds write into slot 0 instead of preventing the
# out-of-bounds computation -- survives without crashing, but is a
# corruption risk in a different, quieter shape. If this project ever
# switches to consuming MiaAI-Lab's build overlay wholesale instead of our
# own recipe-level patches, our v5 fix must take precedence over theirs,
# not the reverse. Their overlay also has no equivalent for the v7
# (vedcsolution kpool-indexer) bug or this #50729 race -- both would still
# need to come from us.
#
# Root cause of #50729 itself: the mamba conv-state left-shift copy (used
# under speculative decoding, when accepted-draft-token counts vary) can
# have `src_block_id == dest_block_id` with a real shift -- i.e. source and
# destination byte ranges overlap WITHIN the same physical block. The old
# kernel (`_copy_mamba_state_block` in `mamba_utils.py`) copied this with a
# naive parallel/tiled memcpy with no memmove ordering guarantee -- under
# concurrent lanes this is a classic read-after-write hazard: a lane can
# overwrite bytes another lane hasn't read yet. Symptom: silent state
# corruption or a downstream illegal-memory-access crash, timing-dependent
# and therefore intermittent -- consistent with this project's own
# Crash B (2026-09-03, decode-phase, 85,373 computed / 101 output tokens,
# no CUDA traceback) and with the broader pattern described in the still-
# OPEN, unresolved vllm-project/vllm#54317 (recurring illegal memory access
# across KDA/MHC/MoE kernels on a 4xB200 fleet -- GLM-5.3-Flash is
# 34/45-layers KDA/mamba). Not confirmed as the exact cause of Crash B (no
# CUDA traceback was captured to prove it), but a highly plausible one
# given our production config uses MTP speculative decoding, and a
# real, already-validated-upstream fix either way.
#
# Fix, three hunks, all in vllm/v1/worker/mamba_utils.py, matching the PR
# exactly:
#   1. `_copy_mamba_state_block`'s DS-conv-dim-first branch: replaced the
#      row-major byte-range loop with a token-major, element-size-aware
#      loop (u16/u32, or per-byte fallback) -- stable row-to-lane ownership
#      makes same-block left shifts memmove-safe while still copying rows
#      in parallel.
#   2. `_copy_mamba_state_block`'s SD-conv branch: added a
#      `src_block_id != dest_block_id or token_bias == 0` check -- distinct
#      blocks and exact self-copies keep the fast vectorized single-CTA
#      memcpy; the one genuinely hazardous case (same block, real shift)
#      instead copies token-by-token low-to-high, where each token's
#      source/destination ranges are disjoint and therefore memmove-safe
#      without any barrier.
#   3. `batch_memcpy_kernel`: added an `is_left_overlap` check
#      (`dst_ptr < src_ptr and dst_ptr + size > src_ptr`) plus a
#      `tl.debug_barrier()` guard before the store, for the same class of
#      hazard in this second, separately-used memcpy kernel.
#
# All three are `@triton.jit`-decorated (helper device function + kernel),
# so patched via the same `_unsafe_update_src()` escape hatch used for the
# v7 fix's Triton-kernel hunk, not plain exec()+reassign.
#
# Validated against a live boot of this exact container before being
# written here (source-text anchors matched byte-for-byte; see recipe
# header investigation above). Full production validation (probe suite +
# a targeted decode-phase long-context test matching Crash B's shape)
# recorded in VALIDATION.md before promotion.
#
# *** PREVIOUS VALIDATED SUMMARY (v7, full results in VALIDATION.md
# "Root-caused and fixed: a SECOND, distinct K-pool/indexer crash bug
# (prefill-triggered)") ***
# *** VALIDATED AND PROMOTED TO DEFAULT 2026-09-03 — full results in
# VALIDATION.md "Real production crash under real traffic" and the
# follow-up root-cause section ***
#
# WHAT'S NEW (v7): fixes a second, DISTINCT crash bug in the same K-pool/
# sparse-indexer subsystem as the v5 fix -- this one PREFILL-triggered
# (large prompts), not generation-length-triggered.
#
# Production (v6) crashed on a real user request: an 18,394-token prompt
# asking for up to 32,000 output tokens, one prefill chunk in
# (num_computed_tokens=7168), KV-cache usage only 4.4% (rules out a
# capacity explanation). Silent "Worker died unexpectedly (exit code:
# None)" -- the same signature class as the v5 K-pool TAIL cache bug, but
# that fix does not apply here: this crash happened with ZERO tokens
# generated yet, still in the first prefill chunk, while v5's bug needed
# ~2.2K *generated* tokens to trigger. Confirmed the v5 patch WAS active
# on the crashed boot -- this is not a failure of that fix, it's a
# SEPARATE bug.
#
# Root cause, independently reported by a different operator on the same
# vLLM PR thread as the v5 fix (vllm-project/vllm#53906, vedcsolution,
# "kpool pool_topk buffers are allocated with torch.empty"): the sparse-
# attention indexer's kpool top-k selection allocates its result buffer
# with `torch.empty` in BOTH the prefill and decode paths
# (`sparse_attn_indexer_kpool.py`). Rows whose logits are fully masked
# (short sequences mixed into a chunked-prefill batch) are never written
# by the top-k kernel, so whatever garbage `torch.empty` returns flows
# into `pool_ids` and from there into the pool-expansion kernel as if it
# were a real, valid pool index -> out-of-bounds gather / silent
# corruption, symptom depending on where the garbage lands. Their reported
# trigger (~25K+ token prompts on their fleet) is close enough to ours
# (18,394 tokens, different TP degree / quant / batching config -- exact
# threshold depends on when a chunk first contains a fully-masked row, not
# a fixed token count) to be confidently the same bug class. Confirmed
# present in this exact container: same code, nearly identical line
# numbers, byte-identical logic to what was reported.
#
# A second surface in the same mechanism: `_expand_pools_and_append_tail_
# kernel` (a Triton kernel in `kpool_compress.py`) guards `pid >= 0` but
# not `pid == pool_len` (one past the last valid pool) -- reachable from
# the same garbage/tail arithmetic, and validated by the same reporter as
# necessary alongside the first fix, not optional.
#
# BOTH surfaces fixed here, matching the reporter's own field-validated
# patch exactly (their fleet: "long-prompt ladder 28K/114K stable across
# repeated boots" after applying both):
#   1. `sparse_attn_indexer_kpool()`: both `torch.empty(...)` allocations
#      (prefill and decode paths) become `torch.full(..., -1, ...)` --
#      `-1` is this file's own existing invalid-index sentinel
#      (`pool_ids >= 0` is already checked elsewhere), so rows the kernel
#      never writes are now correctly skipped instead of leaking garbage.
#   2. `_expand_pools_and_append_tail_kernel`: the `tl.where(pid >= 0, ...)`
#      clamp becomes `tl.where((pid >= 0) & (pid < pool_len), ...)`.
#
# Applied as live monkeypatches -- fix 1 via the same source-text-surgery
# + exec technique used throughout this project; fix 2 differently, since
# Triton's `@triton.jit` decorator requires `inspect.getsourcelines` to
# resolve a real file on disk (a synthetic `exec()` compile target isn't
# enough, and rebuilding a fresh JITFunction from a written-out temp file
# still hit the same requirement via an unrelated tilelang/TVM monkeypatch
# on `inspect.getfile` interacting badly). The actual fix: Triton's own
# `JITFunction._unsafe_update_src(new_src)` -- an intentional, documented
# escape hatch that mutates the existing kernel object's cached source
# text in place and resets its hash to force recompilation, with no file
# resolution involved at all.
#
# Validated with a real ~20K-token single-request prefill (matching the
# crash's actual trigger shape, not a synthetic generation-length test) --
# see VALIDATION.md for the exact run.
#
# *** PREVIOUS VALIDATED SUMMARY (v6, full results in VALIDATION.md
# "max_num_seqs 4 -> 16: validated under long-context concurrent load,
# promoted") ***
# *** VALIDATED AND PROMOTED TO DEFAULT 2026-09-03 — full results in
# VALIDATION.md "max_num_seqs 4 -> 16: validated under long-context
# concurrent load, promoted" ***
#
# WHAT'S NEW: max_num_seqs raised 4 -> 16
#
# The original +171.6% throughput win from this same bump (see the
# forward-pass profiling artifact) was measured entirely on SHORT prompts,
# with KV only 41% used at c=16 -- never validated under real long-context
# load, and this project's own boot logs show a real reason it needed
# checking: this hybrid mamba/MLA architecture pads the attention block
# size up to match the mamba state page size ("Setting attention block
# size to N tokens to ensure attention page size is >= mamba page size"),
# so every KV-cache group pins at least one such block per running request
# regardless of how short that request's content is (see vllm-project/
# vllm#54458 for the general mechanism on other hybrid-model deployments).
#
# Measured directly instead of inferring from log lines
# (`probe_longctx_concurrency.py`): 20K and 60K-token documents at
# concurrency 4/8/16 -- 36/36 requests succeeded, zero failures, server
# stayed healthy throughout. KV-cache usage never exceeded 0.19% even at
# 16 concurrent 60K-token prefills -- the hybrid-page-alignment mechanism
# is real but nowhere near a binding constraint at these realistic
# long-context scales. One mechanism note for future tuning: under
# long-context load, admission is actually gated by
# `max_num_batched_tokens` (7168) serializing prefill, not by
# `max_num_seqs` directly (`num_requests_running` stayed at 1 throughout
# the sweep) -- so this change is a validated SAFETY result (no new risk
# from raising the ceiling), not a proven long-context THROUGHPUT win in
# its own right; the original short-prompt throughput win stands on its
# own merits.
#
# *** PREVIOUS VALIDATED SUMMARY (v5, full results in VALIDATION.md
# "Applied the K-pool tail cache fix (vcruz305's report): v5 promoted")
# ***
# *** VALIDATED AND PROMOTED TO DEFAULT 2026-09-02 — full results in
# VALIDATION.md "Applied the K-pool tail cache fix (vcruz305's report):
# v5 promoted" ***
#
# WHAT'S NEW: fixes a real, reproducible crash-on-long-output bug
#
# vLLM's `glm5next` hybrid model support (this container's commit
# `487ecf187`) has a bug that crashes or silently corrupts output on any
# generation exceeding roughly 2.2K *generated* tokens, independent of
# `max-model-len` — reported and root-caused by vcruz305
# (vllm-project/vllm#53906, 2026-08-30), reproduced on the SAME hardware
# class (DGX Spark GB10, EXL3 pack) and the SAME vLLM commit this recipe
# runs. Confirmed present in this exact container before patching (both
# anchor lines matched, byte-for-byte, in the installed site-packages).
#
# Root cause, two parts:
#   1. `MambaHybridModelState.prepare_attn` (every hybrid-architecture
#      forward pass takes this path) calls `build_attn_metadata(...)`
#      WITHOUT `positions=`, unlike the plain-transformer path in
#      `default.py`. The K-pool tail cache's slot-mapping builder
#      (`compute_kpool_tail_slot_mapping`) needs `positions` to compute
#      each request's own circular tail block; without them it silently
#      falls through to the generic paged mapping, which indexes a
#      one-entry block-table row and produces garbage block ids. Both
#      kpool kernels write through those ids with no bounds check.
#   2. Once `positions` are supplied, `compute_kpool_tail_slot_mapping`
#      runs every step and used to return `slot_mapping.clone()` — a
#      fresh tensor whose address CUDA graph capture bakes in; replay
#      then reads a since-freed/reused buffer (illegal memory access with
#      graphs on, silent corruption without them). Fix: return the
#      caller's own persistent `slot_mapping` buffer in place.
#
# Applied as a live monkeypatch (source-text surgery + `exec`, same
# technique this project uses everywhere site-packages can't be edited
# directly — uid 1000 container, root-owned files), not a file edit or
# rebuild: `MambaHybridModelState.prepare_attn` and
# `compute_kpool_tail_slot_mapping` are re-defined from their own current
# source with the two anchor blocks replaced, then reassigned onto the
# class/module. Both patches assert their anchor text matches exactly
# once before applying — if this vLLM build ever changes these functions,
# the patch fails loudly at boot instead of silently no-op'ing.
#
# Validated: real generations well past the ~2.2K-token crash threshold
# (matching the reporter's own 4,096/8,192-token validation methodology)
# complete cleanly with CUDA graphs on; full probe suite unaffected; the
# v4 FP8 LM head patch coexists cleanly (different files/classes, no
# interaction). See VALIDATION.md for exact numbers.
#
# *** PREVIOUS VALIDATED SUMMARY (v4, full results in VALIDATION.md
# "Promoted: glm-5.3-flash-exl3-v4-vllm.yaml is now the shipped default")
# ***
# *** VALIDATED AND PROMOTED TO DEFAULT 2026-09-02 — full results in
# VALIDATION.md "FP8 LM head attempt #2: scale-aware loader fixes the root
# cause, fully validated" and "Does #2 actually move throughput? Rigorous
# A/B says yes, clearly" ***
#
# WHAT'S NEW: the LM head runs FP8 instead of BF16
#
# `Exl3Config` quantizes only `RoutedExperts` by this checkpoint's own
# declared design (`"scope": "glm53_routed_experts_only"`) — every other
# Linear layer, including the LM head, ran BF16 through v2/v3. This recipe
# adds `ScaleAwareFp8LinearMethod`, an in-process monkeypatch of
# `Exl3Config.get_quant_method` that routes `lm_head` through a subclass of
# vLLM's own `Fp8LinearMethod`, configured for dynamic per-tensor
# quantization from the checkpoint's real BF16 weights (not an offline
# conversion — quantized once at load time, every boot).
#
# Two real bugs in vLLM's stock `Fp8LinearMethod` had to be fixed to get
# here (both root-caused by reading vLLM's actual source, not just its
# docstrings — see VALIDATION.md "FP8 LM head: real hardware win confirmed,
# then a hard end-to-end failure" for the full forensics): (1)
# `create_fp8_weight_parameter()` unconditionally allocates FP8 storage
# regardless of whether the checkpoint is actually FP8-serialized, so the
# stock loader does an UNSCALED cast straight from BF16 into FP8 — this
# checkpoint's `lm_head.weight` sits deep in e4m3's subnormal range
# (max ≈ 0.22), so an unscaled cast destroys nearly all of it; (2) worse,
# `create_fp8_scale_parameter()` initializes `weight_scale` to
# `torch.finfo(torch.float32).min` (≈ -3.4e38) as a sentinel that a plain
# BF16 checkpoint never overwrites, so the real SM121 GEMM kernel was
# running against a garbage weight AND a garbage scale — this is what
# produced the first attempt's `���...` output, not just
# precision loss. `ScaleAwareFp8LinearMethod` allocates weight storage in
# the checkpoint's real dtype (so loading is lossless) and computes an
# actual scale from the loaded weights before quantizing, instead of
# trusting vLLM's checkpoint-already-FP8 assumption.
#
# VALIDATION: full probe suite (sanity/soak/longctx) passed clean, CUDA
# graph capture confirmed, and — critically — a rigorous n=20 A/B
# throughput benchmark (`probe_throughput_ab.py`, fixed prompt, 400-token
# forced completions to average out per-request scheduling noise; the
# project's usual n=3 sanity-bench numbers were too noisy to resolve this)
# found **+8.2% decode throughput** (23.658 → 25.599 tok/s, pooled-SE
# t-stat ≈ 9.95 — not noise) with zero quality regression. Larger than a
# naive kernel-time-share estimate predicted, most likely because MTP-2
# speculative decoding invokes the LM head multiple times per accepted
# step (draft verification + new-token prediction), so its real
# contribution to wall-clock decode time exceeds a single-pass kernel-time
# figure.
#
# SCOPE NOTE — why not attention too: investigated widening this same
# mechanism to attention (the larger remaining bf16 surface, ~30.8% of
# decode kernel time vs. the LM head's ~9.5%) and found it isn't reachable
# by this technique at all: `vllm/models/glm5next/nvidia/{model,kda}.py`
# hardcode `quant_config=None` for every attention sub-projection in both
# the MLA and KDA/mamba families, and — per `_try_load_fp8_attn_proj`'s own
# docstring — this holds even for the OFFICIAL `zai-org/GLM-5.3-Flash` FP8
# checkpoint, which does ship real calibrated FP8 attention weights that
# vLLM actively dequantizes back to BF16 on load. That's a first-party
# quantization-scope decision by the model's own creators (MoE/MLP/LM head
# get FP8, attention stays BF16, unconditionally), not an unimplemented
# vLLM feature or a conservative default — see VALIDATION.md "Attention
# fp8: investigated the 'why', not just the 'how'". Deliberately not
# pursued further: not worth the accuracy risk of going against a
# calibrated decision by the people who trained the model.
#
# GLM53_FP8_LMHEAD_SCALE_MODE=tensor (default, and what's shipped here) —
# per-channel scaling was tested (`channel` mode, same recipe, env
# override) and showed no measurable quality or throughput difference from
# per-tensor on this layer, consistent with the original correctness
# check's finding of zero outlier channels in `lm_head.weight`. Not worth
# the extra complexity; per-tensor is what's promoted.
#
# *** PREVIOUS VALIDATED SUMMARY (v2, full results in VALIDATION.md
# "EXL3 v2: validating four new upstream PRs") ***
# Summary: prefill +14% at 100K-250K context (real, same-day A/B vs. v1,
# both at gmu=0.86 — their own claim was +20-21%, ours is smaller but
# genuine, matching the PR author's own "geometry-sensitive" caveat),
# prefix-cache thinking-toggle fix confirmed clean (toggle now hits warm
# cache, ~3x faster than a cold prefill, indistinguishable from a same-
# effort repeat), indexer workspace rightsizing confirmed exact formula
# match (~1287 MiB reclaimed at our 262K context — scales correctly from
# their 1M-context ~4.5-4.8 GiB headline). Spinwait CPU claim (85.3%
# reduction) inconclusive at this measurement's resolution (docker-stats-
# level, not process-level) — see VALIDATION.md, not disproven, just not
# independently confirmed at that magnitude. Zero regressions: decode
# throughput comparable to v1, full sanity+soak suites pass.
#
# VISION: back ON, matching v1's parity choice. One earlier boot attempt
# hit a KeyboardInterrupt in vLLM's own SIGTERM-during-init handler
# (api_server.py's _interrupt_init) during multi-modal renderer warmup —
# root cause traced to a real signal.signal(SIGTERM, ...) handler, not an
# internal vLLM timeout, but the actual sender was never identified (not
# a Docker healthcheck — none is defined on this image; not this
# project's own tooling — no stop/kill command was issued during that
# window). A clean retest of the identical config immediately after
# booted with vision on with no issue whatsoever ("Multi-modal warmup
# completed in 13.978s", "Readonly multi-modal warmup completed in
# 3.478s") and passed the full sanity+soak suite. Treated as a one-off,
# not a reproducible defect — if it recurs, capture the exact signal
# source before disabling vision again.
#
# MiaAI-Lab merged four perf/correctness PRs today (all 2026-09-01, all on
# `main` at commit c190db1) after the v1 recipe (glm-5.3-flash-exl3-vllm.yaml)
# was validated and put into use. None had reached the published
# `ghcr.io/miaai-lab/...:exl3` image tag as of this recipe (confirmed
# empirically: pulled the tag fresh and grepped the baked-in chat_template.jinja
# for PR63's fix — it wasn't there, so `:exl3` still predates all four merges).
# Built our own image from `main`@c190db1 instead of waiting on a republish:
# `docker build -t glm53-exl3-v2:c190db1 .` from a fresh clone.
#
# The four PRs, read directly from GitHub (title, body, diffs — not the
# social-media summary that prompted this recipe):
#
#   PR77 "perf(exl3): accelerate fat-expert prefill" — purpose-built CUDA
#     kernels (exl3_fat_gemm direct + scatter) for the routed-expert GEMM
#     during prefill. Claimed +20-21% uncached prefill at 8K/100K/300K,
#     decode unchanged. Rigorously benchmarked (45 accepted cold
#     observations, cache-hit exclusion, CV reported, complete separation
#     between on/off arms) but explicitly flagged by the author as
#     "geometry-sensitive" and NOT reproduced on a separate deployment —
#     genuinely worth validating ourselves, not just trusting. Now the
#     shipped default in .env.example (EXL3_FAT_KERNEL=1, tier E2, implies
#     BATCHED+SORTED).
#
#   PR86 "Indexer prefill-workspace right-sizing" — the sparse-MLA indexer's
#     prefill workspace was sized at max_model_len*40 entries (a stock vLLM
#     sizing bug shared with deepseek_v4's call site, which already divides
#     by compress_ratio; glm5next's doesn't). Rightsizing to the legal
#     per-step maximum reclaims KV capacity. THEIR measured ~4.5-4.8 GiB /
#     +26.6% was at max_model_len=1,000,000 — the workspace scales with
#     max_model_len, so at OUR 262,144 context (~26% of theirs) the absolute
#     GiB reclaimed will be proportionally much smaller. Validate directly,
#     don't assume the headline number transfers. Opt-in:
#     GLM53_INDEXER_WORKSPACE=rightsize (default remains `stock`).
#
#   PR63 "emit Reasoning Effort unconditionally to keep the prefix cache" —
#     chat_template.jinja fix, not a flag. The old template gated the
#     "Reasoning Effort" line on thinking_enabled, which rendered ~token 2
#     (before tools) — every thinking on/off toggle diverged the prefix hash
#     at the very start, so vLLM's block-aligned KpoolTailManager cache
#     scored zero credit and re-prefilled from scratch. Fix makes the
#     thinking-off prompt a strict extension of the thinking-on prompt.
#     Comes free with the new image, no config needed.
#
#   PR96 "Add numeric spinwait configuration" — vLLM's shm_broadcast reader
#     spins for a full 1s (sched_yield) after every read before parking on
#     the zmq poller; on GB10's 20 cores those spinners compete with NCCL
#     proxy + engine loop threads. GLM53_SPINWAIT_MS=16 was their own frozen
#     TP=2 sweep result (matches our TP degree, unlike the superseded PR69's
#     TP=4-only 2ms figure): +0.95% median decode, 85.3% less active
#     EngineCore CPU. Opt-in (default `stock` = vLLM's original 1s).
#     Session note (2026-09-02): actually turned this knob on and measured
#     real CPU-seconds via /proc deltas — decode +0.6-3.1% (matches the
#     +0.95% claim) but EngineCore/Worker CPU showed ZERO reduction against
#     the claimed -85.3%. Left enabled (it doesn't hurt and the decode
#     number is real) but the CPU-reduction claim is a tested negative now,
#     not an untested "inconclusive" — see VALIDATION.md.
#
#   A fifth claim ("fairer scheduling... active generations protected from
#   costly mixed cold prefill") maps to GLM53_MIXED_PREFILL_CHUNK=skip,
#   which the v1 recipe already had set — not a new change, just
#   re-highlighted in the social post. No action needed here.
#
# Everything else matches the validated v1 recipe (MTP k=2 — kept as the
# safe speculator; DFlash2 showed two distinct crash classes in production
# use today, unrelated to any of these four PRs, and isn't being
# reintroduced here) so the new variables are isolated, matching this
# project's established one-variable-at-a-time discipline.
#
# Vision/video tower ON, matching v1's parity choice (see the VISION note
# above — the one crash during multimodal warmup didn't reproduce on a
# clean retest). Also dialed gpu_memory_utilization 0.87 -> 0.86 after two
# consecutive startup-time "free memory" failures at 0.87 (see below).
#
# Distribution note: this image is LOCAL to the node it was built on
# (spark-276f / 10.7.0.142), not in a registry. sparkrun's own resource-
# distribution step should ship it to the other node automatically (same
# mechanism used for registry images); if that doesn't work, fall back to
# `docker save | ssh <other-node> docker load`.

recipe_version: "2"
name: GLM-5.3-Flash-EXL3-v21-b12xmoe
description: "PROMOTED (2026-09-19), production. Supersedes v20-upstreamsync. Identical to v20 except the routed-expert MoE path: replaces it with the b12x fused-trellis MoE (GLM53_EXL3_B12X_MOE=1) via a native [2,E,...] weight-layout change that makes the load genuinely zero-copy (measured 71 MiB/rank vs. an earlier, rejected post-load-adapter approach's 47.25 GiB/rank). Validated: 30/30 correctness parity cases (cosine 1.0) vs. this fork's own reference loop, near-parity-to-7%-faster throughput same-harness, and a direct real-traffic measurement of the one known limitation (b12x's correctness mode can't replicate this fork's +-10.0 SwiGLU clamp; measured real-world rate ~1-in-3.5M to 1-in-15M activations, accepted as a documented rare limitation, not a blocker). See recipes/VALIDATION.md's 'b12x fused-trellis MoE ... validated, v21-b12xmoe' and 'SwiGLU-clamp real-traffic measurement' sections for full background. Rollback target: v20-upstreamsync (GLM53_EXL3_B12X_MOE=1 simply unset restores the identical pre-b12x behavior on this same image family if needed, but the clean rollback is the v20 image itself)."

model: Mia-AiLab/GLM-5.3-Flash-EXL3-TR3-4bpw
runtime: vllm
container: glm53-exl3-v21-b12xmoe:local

min_nodes: 2

cluster_config:
  resolved_model_path: /models

executor_config:
  entrypoint: ""
  auto_remove: false

env:
  # --- image / GPU arch (GB10 = SM121) ---
  TORCH_CUDA_ARCH_LIST: 12.1a
  FLASHINFER_CUDA_ARCH_LIST: 12.1a
  FLASHINFER_DISABLE_VERSION_CHECK: "1"
  # --- GB10 unified-memory hygiene ---
  PYTORCH_CUDA_ALLOC_CONF: expandable_segments:True
  # --- TileLang JIT cache persistence (2026-09-05) ---
  # Found by cross-reading Enntity/sparkglm (a sibling GLM-5.3-Flash/2xGB10
  # project sharing our checkpoint and much of our GLM53_* patch lineage):
  # their start.sh carries "Overlay FS ~/.triton and ~/.tilelang die on
  # container recreate (TP=2 JIT stall -> 600s NCCL watchdog). Persist next
  # to the vLLM cache." Checked our own image against that claim directly:
  # every other JIT/compile cache is already pinned under the
  # sparkrun-persisted /cache/runtime mount by the image itself
  # (TRITON_CACHE_DIR, TORCHINDUCTOR_CACHE_DIR, FLASHINFER_CACHE_DIR,
  # CUTE_DSL_CACHE_DIR, FLASH_ATTENTION_CUTE_DSL_CACHE_DIR all point there)
  # -- TILELANG_CACHE_DIR is the one exception, absent from that list. It
  # falls back to TileLang's own default, `~/.tilelang/cache`; the
  # container's HOME is /tmp, so that resolves to /tmp/.tilelang/cache --
  # inside the ephemeral container filesystem, gone on every recreate.
  # Confirmed live on the v8@0.84 production boot: that ephemeral path
  # already held compiled kernels including the mhc_pre_big_fuse_with_norm_
  # tilelang family -- the exact kernel whose first-ever JIT compile
  # preceded the fourth production crash (VALIDATION.md, "A FOURTH
  # production crash... strongest lead yet on the #54317 pattern") by 20
  # seconds, on a TP=2 boot that had been serving for 18 minutes. That
  # crash's own writeup found "no code-level lead to fix" -- this is the
  # first one. Redirecting the cache doesn't prevent a first-ever compile
  # of a given kernel shape, but it means that cost is paid once per unique
  # shape across this image's whole lifetime instead of once per container
  # recreate, which is exactly the boot-to-boot pattern PR77/86/96's own
  # Triton cache already relies on. TILELANG_TMP_DIR is not set here
  # because TileLang's own default derives it from TILELANG_CACHE_DIR
  # (`os.path.join(Environment.TILELANG_CACHE_DIR, "tmp")`).
  TILELANG_CACHE_DIR: /cache/runtime/tilelang
  # --- long boot tolerance ---
  VLLM_ENGINE_READY_TIMEOUT_S: "3600"
  # --- NCCL diagnostics (fabric selection itself is sparkrun-detected) ---
  NCCL_DEBUG: WARN
  TORCH_NCCL_ASYNC_ERROR_HANDLING: "1"
  # --- NCCL fixed channel count: A/B'd and REJECTED (2026-09-18) ---
  # Entrpi/glm-5.3-flash-exl3-2x-spark measured a win pinning 8 channels on
  # their identical 200GbE RoCE link topology (3.38->2.96ms for a 37.7MB
  # all-reduce; their own data says this is decode-neutral end-to-end,
  # sub-1% either way -- see VALIDATION.md's Entrpi triage). Tested here
  # with this fork's probe_throughput_ab.py (n=20 each arm, identical
  # prompt/config): baseline (no pinning) median 31.767 tok/s (stdev
  # 0.697); with NCCL_MIN/MAX_NCHANNELS=8, median 31.07 (stdev 0.819) -- a
  # ~2.3% DECREASE, not an improvement, and the per-arm spread is tight
  # enough that this isn't just overlapping noise. Also required a real
  # fix to land at all: NCCL_MIN_NCHANNELS (unlike its MAX sibling, set in
  # the same block) silently did not survive into the Worker_TP process's
  # exec-time environment -- same unexplained env-stripping class as
  # _fix_glm53_mixed_prefill_env, confirmed via /proc/<pid>/environ on a
  # freshly spawned worker, root cause not found (checked vLLM source: the
  # only code touching NCCL_MIN_NCHANNELS is batch_invariant.py, gated
  # behind envs.VLLM_BATCH_INVARIANT, confirmed inactive here). Not worth
  # carrying the added complexity (a second env-stripping workaround) for
  # a result that didn't show a benefit. See
  # entrpi_triage_2026_09_18.md / nccl_channel_ab_2026_09_18.md.
  # --- EXL3-specific (unchanged from v1) ---
  EXL3_FUSED_MOE: "1"
  GLM53_SUPPRESS_STOPS_IN_REASONING: "1"
  GLM53_MIXED_PREFILL_CHUNK: skip
  # --- v21-b12xmoe PROMOTED (2026-09-19) ---
  # GLM53_EXL3_B12X_MOE=1: the b12x fused-trellis MoE path, the whole
  # point of this recipe, now on. Validated: 30/30 correctness parity
  # (cosine 1.0) vs. this fork's own reference loop; memory zero-copy
  # confirmed (data_ptr() equality, 71 MiB/rank vs. the rejected
  # post-load-adapter approach's 47.25 GiB/rank); throughput near-parity-
  # to-7%-faster same-harness vs. the real grouped-tier fused path;
  # SwiGLU-clamp risk measured directly on real traffic and accepted as a
  # documented, rare (~1-in-3.5M to 1-in-15M activations) limitation, not
  # a blocker -- see recipes/VALIDATION.md's "SwiGLU-clamp real-traffic
  # measurement" and "b12x fused-trellis MoE ... validated" sections.
  # GLM53_EXL3_CLAMP_DIAG left off (default "0") -- its one job (measuring
  # real-traffic clamp behavior) is done; no reason to carry it live.
  GLM53_EXL3_B12X_MOE: "1"
  # --- NEW in v2: PR77 fat-expert prefill kernel ---
  # Tier E2 (direct + scatter CUDA kernel). Implies EXL3_FAT_BATCHED=1 and
  # EXL3_FAT_SORTED=1. Now the shipped default in upstream .env.example.
  EXL3_FAT_KERNEL: "1"
  # Row tiles OFF — matches PR77's own benchmark protocol exactly
  # ("MNBT 2048; fused MoE on; row tiles off").
  EXL3_MOE_ROW_TILE: "0"
  # --- NEW in v2: PR86 indexer workspace rightsizing ---
  GLM53_INDEXER_WORKSPACE: rightsize
  # --- NEW in v2: PR96 spinwait tuning ---
  # Their own frozen TP=2 sweep result (matches our TP degree).
  GLM53_SPINWAIT_MS: "16"
  # --- NEW in v4: FP8 LM head scale mode ---
  # "tensor" (default, shipped) or "channel" (tested, no measurable
  # difference — see recipe header). Kept as a runtime switch so the two
  # can still be A/B'd without a rebuild if this checkpoint's lm_head.weight
  # ever changes in a way that makes per-channel matter.
  GLM53_FP8_LMHEAD_SCALE_MODE: tensor
  # --- NEW in v9-fatfork: sparkglm's grouped-prefill fat-expert kernel ---
  # Requires this recipe's custom-built `glm53-exl3-fatfork:local` image
  # (see recipe header) -- the symbol does not exist in the stock
  # glm53-exl3-v2:c190db1 image. Grouped-prefill only; EXL3_DECODE_COOP_K4
  # must never be set (see overlay/exl3.py's header comment in the build
  # tree for why that's safe to leave inert rather than absent).
  EXL3_GROUPED_PREFILL_K4: "1"
  # --- v12-combined: MiaAI-Lab's own E3 grouped-MoE kernel (EXL3_FAT_GROUPED) ---
  # New top-level tier above sparkglm's kernel-tier dispatch; falls through to
  # sparkglm's kernel if a layer is ineligible. Being tested at our real context
  # range (up to 262144) since E3's own claim stops at 100K. See VALIDATION.md.
  EXL3_FAT_GROUPED: "1"
  # --- v14-flashkda: force vLLM #55737's FlashKDA chunked-prefill backend on
  # for this test boot ("What's next" item 2). Fails loudly at boot if this
  # config doesn't meet the eligibility gate (SM90/SM10x/SM12x + bf16 +
  # head_dim==128 + bounded gate), rather than silently falling back to
  # Triton -- deliberate, matches this project's fail-closed convention.
  # NOT independently benchmarked on SM121/GB10 yet -- the PR's own real
  # numbers are GB300/SM100 only. This build does NOT include the
  # v13-moegemm gate-dedup patch (built from v12-combined, one variable at
  # a time) -- combine only after each is individually validated.
  GLM53_FLASHKDA_PREFILL: "1"
  # DEFAULT ON (2026-09-11): MiaAI-Lab's FP8 weight-only (Marlin) dense-
  # projections path (GLM53_DENSE_FP8, ported from v16-densefp8, originally
  # commit eb0da1d -- see recipes/build/glm53-exl3-v20-upstreamsync/
  # Dockerfile and NOTES.md for the port). Re-measured against this exact
  # image, not the stale v15-combined baseline the original A/B used:
  # decode +14-17%, prefill @64K -15.6% -- the tradeoff holds, unmoved by
  # any kernel work since (E3, GB10 router-GEMM, FlashKDA, MoE-gate-dedup).
  # Given the real prefill throughput gained since v1 (~860-900 -> ~1,500-
  # 1,700+ tok/s) and this fleet's ~85/15 heavy/short traffic split plus
  # growing agentic use, the decode win is worth the prefill give-back here.
  # Coherence/accuracy side-by-side (6 representative prompts incl. a CJK
  # translation, the project's known corruption-risk case) confirmed clean
  # -- no garbling, no wrong answers, only the expected paraphrase-level
  # drift any precision change causes under greedy decoding. Still
  # PROVISIONAL per upstream's own commit message ("changes target
  # numerics"), not a full KLD panel. For prefill-dominated workloads that
  # want the old tradeoff back, use the maxprefill sibling recipe instead:
  # glm-5.3-flash-exl3-v20-upstreamsync-maxprefill-vllm.yaml (same image,
  # this one variable unset). See recipes/VALIDATION.md, "Dense-FP8
  # promoted to default, maxprefill sibling added".
  GLM53_DENSE_FP8: "dense,kda"
  # 2026-09-15 upstream-check round: decode hygiene, backported from
  # MiaAI-Lab PR #51 (overlay/patch_default_max_new_tokens.py). Without a
  # server-side default, a request that omits max_tokens gets vLLM's own
  # fallback of max_model_len - prompt_len -- up to our full 262144-token
  # ceiling on a short prompt, large enough for one such client to decode
  # until it preempts every other session via KV pressure. This bounds
  # ONLY omitted requests; an explicit client max_tokens always wins, and
  # this is a generous ceiling, not a real output-length cap for any
  # legitimate completion. See recipes/VALIDATION.md, "TP=4 vs. 2x TP=2
  # assessment... 2026-09-14" / the 2026-09-15 follow-up entry.
  DEFAULT_MAX_NEW_TOKENS: "65536"

defaults:
  port: 8000
  host: 0.0.0.0
  served_model_name: glm-5.3-flash-exl3-v2
  tensor_parallel: 2
  model_path: /models/models--Mia-AiLab--GLM-5.3-Flash-EXL3-TR3-4bpw/snapshots/024db9f7e9871e8efdf21538ba55af7442be3cd5
  quantization: exl3
  max_model_len: 262144
  max_num_seqs: 16
  # Dialed back from 0.87 (2026-09-01): two consecutive v2 boots failed at
  # 0.87 with "Free memory ... 104.93/105.31 GiB ... less than desired ...
  # 105.87 GiB" — a razor-thin, real shortfall (not fixed by a fresh
  # prelaunch_flush.sh between attempts), so this isn't page cache. Chose
  # 0.86, not this project's usual 0.85 fallback: MiaAI-Lab's own docs cite
  # 0.86 as their tested fallback for tighter-margin nodes ("GPU_MEM_UTIL=
  # 0.86 ... fits with margin"), and v1's own earlier failures at 0.85 were
  # a *different* problem (post-weight-load KV-sizing shortfall, not this
  # startup free-memory check) — 0.86 splits the difference with informed
  # backing rather than guessing.
  #
  # Dropped 0.86 -> 0.84 (2026-09-04): the 6.17.0-1032-nvidia kernel bump
  # (see VALIDATION.md) shrank the head-node startup free-memory margin
  # enough that earlyoom killed the head twice in a row at 0.86. Retested
  # after fully rebooting both nodes a second time (fresh kernel/driver
  # state, not just a fresh container) to rule out post-reboot settling —
  # the head node still plateaued at ~1.3% mem-avail / 80.03% swap-free,
  # 0.03pp from earlyoom's 80.00% kill trigger, i.e. still on the same
  # knife-edge, just not tipped over yet. Not treating a lucky non-kill as
  # cleared; 0.84 is the validated safe value on this kernel.
  #
  # Dropped 0.84 -> 0.82 (2026-09-17): both hosts were moved off the pinned
  # 6.17.0-1032-nvidia kernel onto 7.0.0-1019-nvidia by a manual `sudo
  # shutdown -r now` (apt had just unpacked the new kernel package; nothing
  # in this project authorized or validated the jump). Same signature as the
  # 0.86->0.84 drop above repeated almost exactly: booted clean, served one
  # real request, then earlyoom SIGTERM'd the head node's API server ~3 min
  # later (`badness 984, VmRSS 3464 MiB`) -- a graceful-looking shutdown in
  # the vLLM log because earlyoom's SIGTERM let it unwind cleanly, not a
  # crash. 0.84's margin was tuned against 1032's memory-accounting
  # behavior specifically; nothing here says 7.0.0-1019 behaves the same.
  # 0.82 is an unvalidated first guess, same-size step as the prior kernel-
  # bump fix -- needs its own soak before calling it safe. Revert to 0.84
  # if/when the fleet goes back to 6.17.0-1032.
  #
  # Raised 0.82 -> 0.83 (2026-09-18): direction reversal from the trend
  # above -- explicit user call, not a stability fix. Same day, this fleet
  # had a real earlyoom kill of the head's VLLM::Worker_TP (node_1, still on
  # 7.0.0-1019-nvidia, unrelated to this setting specifically -- see
  # b12x_moe_adapter_infeasible_2026_09_18.md's sibling incident notes) and
  # both hosts were independently observed sitting at a chronic ~4.3-4.9%
  # mem-avail margin even while otherwise healthy. Raising gmu trades back
  # some of the just-recovered headroom for KV cache: measured against the
  # actual post-recovery boot (node_0/head, real profiled numbers, not the
  # static pre-launch estimate) -- fixed cost (weights+non-torch 81.64 GiB
  # + peak activation 6.67 GiB + CUDA graph 0.45 GiB) is unaffected by gmu,
  # so the usable-budget delta lands entirely on KV cache: 0.82's 11.48 GiB
  # / 1,250,457 tokens / 4.77x concurrency @ 262,144 tokens/request becomes
  # ~12.70 GiB / ~1.38M tokens / ~5.28x at 0.83 (+10.6%), costing back
  # ~1.22 GiB of the margin this fleet has twice been OOM-killed over.
  #
  # Dropped 0.83 -> 0.82 (2026-09-20): the watch-for-a-repeat above found
  # one. node_0 (10.7.0.87, the chronically thinner-margin host, as
  # flagged) took two real earlyoom SIGTERM kills at 0.83 within the same
  # ~3-hour window: first the API server process (19:11:40 UTC, badness
  # 984, VmRSS 3353 MiB), then -- after a stop/flush/relaunch recovery --
  # a Worker_TP process on the very next boot (17:55:00 local, badness
  # 985, VmRSS 2367 MiB, plus a `kill failed: Timer expired` on top of
  # it). Both during otherwise-idle/light real-request load (a
  # concurrency sweep to max_num_seqs=16 shortly before the first kill is
  # the likely proximate trigger, not a steady-state baseline problem,
  # but the margin at 0.83 clearly cannot absorb it). Two kills on the
  # same host in one afternoon is not "still on the knife-edge, not
  # tipped over" -- it tipped. Reverting the 0.82->0.83 raise; 0.82 was
  # already the last soaked-if-unvalidated value on this kernel before
  # that raise. Re-raising to 0.83 again would need its own fresh
  # justification and soak, not just "it was fine for a while."
  gpu_memory_utilization: 0.82
  kv_cache_dtype: fp8
  # Raised 2048 -> 7168 (2026-09-02): matches upstream's own current
  # production value (discovered via the PR80 merge conflict in README.md
  # — their doc says "E2 keep 2026-09-01", chosen specifically alongside
  # the fat-expert prefill kernel). Measured on this cluster: +28.4%
  # prefill at 100K tokens (933->1199 tok/s), +19.5% at 250K (1048->1253
  # tok/s), zero decode regression on prose OR structured content (25.68
  # tok/s prose, 31.65 tok/s median / 96.57% MTP-2 accept on a counting-
  # task structured probe — see VALIDATION.md), full sanity+soak (incl.
  # concurrent waves) passed. A substantial, real, independently-confirmed
  # gain. Contrast with the NVFP4 lane, which crashed raising this same
  # knob to just 3584 — direct, measured confirmation that EXL3's smaller
  # memory footprint is real headroom, not just theory.
  max_num_batched_tokens: 7168
  limit_mm: '{"image":4,"video":1}'
  default_chat_template_kwargs: '{"enable_thinking": false}'
  # MTP k=2 — matches v1's safe default. DFlash2 not reintroduced here;
  # its two crash classes today were unrelated to any of these four PRs.
  speculative_config: '{"method":"mtp","num_speculative_tokens":2}'

command: |
  cat > /tmp/glm53_v8_shim.py <<'PYSHIM'
  # FP8 LM head monkeypatch. See recipe header for full rationale and
  # VALIDATION.md for the complete investigation (two v2/v3 boot bugs
  # root-caused: unscaled FP8 cast + uninitialized -3.4e38 weight_scale
  # sentinel; both fixed by ScaleAwareFp8LinearMethod below).
  #
  # Multiprocessing-boundary handling matches every other in-process patch
  # in this project (spinwait, fp8-lmhead v1/v2): patch at IMPORT TIME
  # (unconditional, runs in every process that re-imports this file,
  # spawned or not); only the actual `main()` launch stays behind the
  # `if __name__ == "__main__"` guard. A patch gated the same way as the
  # launch call silently never reaches the multiprocessing.spawn-reimported
  # API-server child that EngineCore/Worker actually fork from.
  import inspect
  import os
  import sys

  import torch

  from vllm.model_executor.layers.linear import LinearBase
  from vllm.model_executor.layers.vocab_parallel_embedding import ParallelLMHead
  from vllm.model_executor.layers.quantization.exl3 import Exl3Config
  from vllm.model_executor.layers.quantization.fp8 import (
      Fp8Config,
      Fp8LinearMethod,
      MarlinFP8ScaledMMLinearKernel,
      init_fp8_linear_kernel,
  )
  from vllm.model_executor.parameter import ModelWeightParameter
  from vllm.model_executor.utils import replace_parameter

  _SCALE_MODE = os.environ.get("GLM53_FP8_LMHEAD_SCALE_MODE", "tensor")
  assert _SCALE_MODE in ("tensor", "channel"), _SCALE_MODE

  _fp8_config = Fp8Config(
      is_checkpoint_fp8_serialized=False,
      activation_scheme="dynamic",
      weight_block_size=None,
  )


  class ScaleAwareFp8LinearMethod(Fp8LinearMethod):
      """Fp8LinearMethod for on-the-fly quantization from a genuinely bf16
      checkpoint. Upstream's create_weights/process_weights_after_loading
      assume the checkpoint is ALREADY fp8-serialized (fp8 weight storage
      from the start + a real `*.weight_scale` tensor on disk); neither
      holds here. This class fixes both: allocate weight storage in the
      checkpoint's real dtype so the standard loader does a normal,
      lossless copy, then compute a REAL scale from the actual loaded
      weights ourselves before quantizing.
      """

      def create_weights(
          self,
          layer,
          input_size_per_partition,
          output_partition_sizes,
          input_size,
          output_size,
          params_dtype,
          **extra_weight_attrs,
      ):
          assert not self.block_quant, "scale-aware path is per-tensor/channel dynamic only"
          output_size_per_partition = sum(output_partition_sizes)
          weight_loader = extra_weight_attrs.get("weight_loader")
          layer.logical_widths = output_partition_sizes
          layer.input_size_per_partition = input_size_per_partition
          layer.output_size_per_partition = output_size_per_partition
          layer.orig_dtype = params_dtype
          layer.weight_block_size = None

          # params_dtype (bf16), NOT fp8 -- the whole point of this override.
          weight = ModelWeightParameter(
              data=torch.empty(
                  output_size_per_partition, input_size_per_partition,
                  dtype=params_dtype,
              ),
              input_dim=1,
              output_dim=0,
              weight_loader=weight_loader,
          )
          layer.register_parameter("weight", weight)
          # Deliberately NOT creating a weight_scale parameter here -- see
          # process_weights_after_loading, which computes and attaches a
          # real one once actual weights exist.

          self.fp8_linear = init_fp8_linear_kernel(
              activation_quant_key=self.activation_quant_key,
              weight_quant_key=self.weight_quant_key,
              weight_shape=(output_size_per_partition, input_size_per_partition),
              input_dtype=self.input_dtype,
              out_dtype=self.out_dtype,
              module_name=self.__class__.__name__,
          )
          self.use_marlin = isinstance(self.fp8_linear, MarlinFP8ScaledMMLinearKernel)

      def process_weights_after_loading(self, layer) -> None:
          assert not self.use_marlin, "scale-aware path assumes real SM121 FP8 tensor cores"
          w = layer.weight.data  # real bf16 weights, correctly loaded by now
          w32 = w.to(torch.float32)

          if _SCALE_MODE == "channel":
              # Per output row. w32 is [N, K] here (pre-transpose), so the
              # scale used to DIVIDE it must be [N, 1] to broadcast per row.
              scale_div = (w32.abs().amax(dim=1, keepdim=True) / 448.0).clamp(min=1e-12)
          else:
              # Per-tensor: a single scalar for the whole weight.
              scale_div = (w32.abs().amax() / 448.0).clamp(min=1e-12).reshape(1)

          wq = (w32 / scale_div).clamp(-448.0, 448.0).to(torch.float8_e4m3fn)

          # weight_scale is stored as [N, 1] for channel mode -- NOT [1, N]
          # -- even after weight is transposed to [K, N] below. Confirmed
          # from vLLM's own kernel-level padding code, not just the
          # cutlass_scaled_mm docstring's broadcast description, which
          # doesn't literally hold for [N,1] against [K,N]: the native
          # binding applies it as N per-output-column scales directly.
          scale_store = scale_div

          replace_parameter(layer, "weight", wq.t())
          layer.weight_scale = torch.nn.Parameter(scale_store.to(torch.float32), requires_grad=False)
          layer.input_scale = None

          print(
              f"[glm53-fp8-lmhead-v4] quantized weight scale_mode={_SCALE_MODE} "
              f"scale_shape={tuple(scale_store.shape)} scale_range=[{scale_store.min().item():.6g}, "
              f"{scale_store.max().item():.6g}] weight_max_before={w32.abs().max().item():.4f}",
              file=sys.stderr, flush=True,
          )

          # Stock kernel-level post-processing from here (16-alignment
          # padding etc.) -- everything downstream of weight prep is
          # unmodified vLLM.
          self.fp8_linear.process_weights_after_loading(layer)


  _original_get_quant_method = Exl3Config.get_quant_method


  def _patched_get_quant_method(self, layer, prefix):
      if isinstance(layer, (LinearBase, ParallelLMHead)) and "lm_head" in prefix:
          print(f"[glm53-fp8-lmhead-v4] routing '{prefix}' ({type(layer).__name__}) "
                f"through ScaleAwareFp8LinearMethod (dynamic, scale_mode={_SCALE_MODE})",
                file=sys.stderr, flush=True)
          return ScaleAwareFp8LinearMethod(_fp8_config)
      return _original_get_quant_method(self, layer, prefix)


  Exl3Config.get_quant_method = _patched_get_quant_method
  print(f"[glm53-fp8-lmhead-v4] Exl3Config.get_quant_method patched (pid={os.getpid()}, "
        f"__name__={__name__!r}, scale_mode={_SCALE_MODE!r})", file=sys.stderr, flush=True)


  # --- K-pool tail cache out-of-bounds fix (vcruz305, vllm-project/vllm#53906) ---
  # See recipe header for the full root cause. Applied as source-text
  # surgery + exec, mirroring vcruz305's own published file-edit patch
  # (scripts/patch_kpool_tail_positions.py) exactly, just applied live
  # instead of on disk.
  import textwrap

  import vllm.v1.worker.gpu.model_states.mamba_hybrid as _mamba_hybrid_mod
  import vllm.v1.attention.backends.mla.indexer as _indexer_mod
  import vllm.v1.worker.mamba_utils as _mamba_utils_mod


  def _patch_kpool_tail_bug():
      # Fix 1: pass positions= through the hybrid attn-metadata path.
      src = textwrap.dedent(
          inspect.getsource(_mamba_hybrid_mod.MambaHybridModelState.prepare_attn)
      )
      anchor = (
          "        dcp_local_seq_lens=input_batch.dcp_local_seq_lens,\n"
          "        model_specific_attn_metadata=mamba_attn_metadata,\n"
          "        for_cudagraph_capture=for_capture,\n"
          "        rswa_prefix_lens=input_batch.prompt_lens,\n"
          "    )\n"
      )
      patched = (
          "        dcp_local_seq_lens=input_batch.dcp_local_seq_lens,\n"
          "        # positions= added by the kpool-tail-bug patch -- see\n"
          "        # recipe header. Without this the K-pool tail builder\n"
          "        # writes out of bounds past ~2.2K generated tokens.\n"
          "        positions=input_batch.positions,\n"
          "        model_specific_attn_metadata=mamba_attn_metadata,\n"
          "        for_cudagraph_capture=for_capture,\n"
          "        rswa_prefix_lens=input_batch.prompt_lens,\n"
          "    )\n"
      )
      count = src.count(anchor)
      assert count == 1, (
          f"prepare_attn anchor found {count} times, expected 1 -- "
          "this vLLM build's prepare_attn changed, re-derive the patch"
      )
      src = src.replace(anchor, patched, 1)
      exec(compile(src, "<kpool_tail_patch:prepare_attn>", "exec"), _mamba_hybrid_mod.__dict__)
      _mamba_hybrid_mod.MambaHybridModelState.prepare_attn = _mamba_hybrid_mod.__dict__["prepare_attn"]

      # Fix 2: return slot_mapping in place, not a fresh clone (CUDA-graph-safe).
      src2 = inspect.getsource(_indexer_mod.compute_kpool_tail_slot_mapping)
      anchor2 = "    out = slot_mapping.clone()\n"
      count2 = src2.count(anchor2)
      assert count2 == 1, (
          f"compute_kpool_tail_slot_mapping anchor found {count2} times, expected 1 -- "
          "this vLLM build's function changed, re-derive the patch"
      )
      src2 = src2.replace(
          anchor2,
          "    # in place -- see recipe header fix 2\n    out = slot_mapping\n",
          1,
      )
      exec(compile(src2, "<kpool_tail_patch:compute_kpool_tail_slot_mapping>", "exec"), _indexer_mod.__dict__)
      _indexer_mod.compute_kpool_tail_slot_mapping = _indexer_mod.__dict__["compute_kpool_tail_slot_mapping"]

      print(
          f"[glm53-kpool-tail-fix] patched MambaHybridModelState.prepare_attn "
          f"(positions=) and compute_kpool_tail_slot_mapping (in-place) "
          f"(pid={os.getpid()}, __name__={__name__!r})",
          file=sys.stderr, flush=True,
      )


  _patch_kpool_tail_bug()


  # --- K-pool INDEXER prefill crash fix (vedcsolution, vllm-project/vllm#53906) ---
  # See recipe header for the full root cause. Distinct bug from the tail-cache
  # fix above: this one is prefill-triggered (large prompts), not
  # generation-length-triggered.
  def _patch_kpool_indexer_prefill_bug():
      # Force glm5next's own import chain to fully resolve first -- importing
      # sparse_attn_indexer_kpool as the very first reference to it hits a
      # circular import (it's normally reached via the model registry's own
      # import order, never as a standalone first import).
      import vllm.models.glm5next.nvidia.attention  # noqa: F401
      import vllm.model_executor.layers.sparse_attn_indexer_kpool as _kpool_mod
      import vllm.models.glm5next.nvidia.ops.kpool_compress as _compress_mod

      # Fix 1: both torch.empty(...) pool_topk allocations (prefill + decode)
      # become torch.full(..., -1, ...). -1 is this file's own existing
      # invalid-index sentinel; rows the top-k kernel never writes (fully
      # masked logits -- short sequences mixed into a chunked-prefill batch)
      # are now correctly skipped downstream instead of leaking torch.empty
      # garbage into pool_ids.
      src = inspect.getsource(_kpool_mod.sparse_attn_indexer_kpool)

      anchor_prefill = (
          "                pool_topk = torch.empty(\n"
          "                    (num_rows, select_k), dtype=torch.int32, device=logits.device\n"
          "                )\n"
      )
      replacement_prefill = (
          "                # [glm53-kpool-indexer-fix] -1 sentinel, not garbage --\n"
          "                # see recipe header. Rows the top-k kernel never writes\n"
          "                # (fully masked logits) must not leak torch.empty garbage.\n"
          "                pool_topk = torch.full(\n"
          "                    (num_rows, select_k), -1, dtype=torch.int32, device=logits.device,\n"
          "                )\n"
      )
      count_p = src.count(anchor_prefill)
      assert count_p == 1, f"prefill pool_topk anchor found {count_p} times, expected 1 -- re-derive the patch"
      src = src.replace(anchor_prefill, replacement_prefill, 1)

      anchor_decode = (
          "            pool_topk = torch.empty(\n"
          "                (num_rows, select_k), dtype=torch.int32, device=logits.device\n"
          "            )\n"
      )
      replacement_decode = (
          "            # [glm53-kpool-indexer-fix] -1 sentinel, not garbage -- see\n"
          "            # recipe header.\n"
          "            pool_topk = torch.full(\n"
          "                (num_rows, select_k), -1, dtype=torch.int32, device=logits.device,\n"
          "            )\n"
      )
      count_d = src.count(anchor_decode)
      assert count_d == 1, f"decode pool_topk anchor found {count_d} times, expected 1 -- re-derive the patch"
      src = src.replace(anchor_decode, replacement_decode, 1)

      exec(compile(src, "<kpool_indexer_patch:sparse_attn_indexer_kpool>", "exec"), _kpool_mod.__dict__)
      _kpool_mod.sparse_attn_indexer_kpool = _kpool_mod.__dict__["sparse_attn_indexer_kpool"]

      # Fix 2: the pool-expansion Triton kernel's tl.where(pid >= 0, ...) clamp
      # guards negative pid but not pid == pool_len (one past the last valid
      # pool), reachable from the same garbage/tail arithmetic. Mutate the
      # EXISTING JITFunction's cached source in place via Triton's own
      # documented escape hatch (_unsafe_update_src) -- rebuilding a fresh
      # JITFunction via @triton.jit requires inspect.getsourcelines to resolve
      # a real file on disk, which a synthetic exec() target can't satisfy and
      # which an unrelated tilelang/TVM monkeypatch on inspect.getfile breaks
      # even when written out to a real temp file.
      kernel = _compress_mod._expand_pools_and_append_tail_kernel
      anchor_kernel = "    hist_out = tl.where(pid >= 0, hist_val, -1)\n"
      count_k = kernel.src.count(anchor_kernel)
      assert count_k == 1, f"expand-kernel anchor found {count_k} times, expected 1 -- re-derive the patch"
      new_kernel_src = kernel.src.replace(
          anchor_kernel,
          "    hist_out = tl.where((pid >= 0) & (pid < pool_len), hist_val, -1)\n",
          1,
      )
      kernel._unsafe_update_src(new_kernel_src)

      print(
          f"[glm53-kpool-indexer-fix] patched sparse_attn_indexer_kpool "
          f"(pool_topk sentinel) and _expand_pools_and_append_tail_kernel "
          f"(pool_len clamp) (pid={os.getpid()}, __name__={__name__!r})",
          file=sys.stderr, flush=True,
      )


  _patch_kpool_indexer_prefill_bug()


  # --- Mamba state-copy race fix (backport of vllm-project/vllm#50729) ---
  # See recipe header for the full root cause. Three Triton hunks in
  # vllm/v1/worker/mamba_utils.py, all mutated via _unsafe_update_src()
  # (same technique as the v7 fix's Triton-kernel hunk) since @triton.jit
  # can't rebuild a fresh JITFunction from synthetic/temp-file source in
  # this environment.
  def _patch_mamba_state_copy_race():
      kernel_copy = _mamba_utils_mod._copy_mamba_state_block
      src = kernel_copy.src

      # Hunk 1: DS-conv-dim-first branch -- row-major byte loop becomes a
      # token-major, element-size-aware loop (stable row-to-lane ownership
      # makes same-block left shifts memmove-safe).
      anchor1 = (
          "        per_row_bytes = (conv_width - token_bias).to(tl.int64) * state_elem_size\n"
          "        bias_bytes = token_bias.to(tl.int64) * state_elem_size\n"
          "        src_block_addr = state_base_addr + src_block_id * state_block_stride\n"
          "        offsets = tl.arange(0, COPY_BLOCK_SIZE)\n"
          "        for d in range(0, dim_rows):\n"
          "            row_src = src_block_addr + d * row_stride + bias_bytes\n"
          "            row_dst = dst_addr + d * row_stride\n"
          "            for i in range(0, per_row_bytes, COPY_BLOCK_SIZE):\n"
          "                mask = (i + offsets) < per_row_bytes\n"
          "                curr_src = (row_src + i + offsets).to(tl.pointer_type(tl.uint8))\n"
          "                curr_dst = (row_dst + i + offsets).to(tl.pointer_type(tl.uint8))\n"
          "                data = tl.load(curr_src, mask=mask)\n"
          "                tl.store(curr_dst, data, mask=mask)\n"
      )
      replacement1 = (
          "        src_block_addr = state_base_addr + src_block_id * state_block_stride\n"
          "        offsets = tl.arange(0, COPY_BLOCK_SIZE)\n"
          "\n"
          "        # [glm53-mamba-race-fix] Stable row-to-lane ownership makes left\n"
          "        # shifts memmove-safe while exposing the dimension rows in\n"
          "        # parallel -- see recipe header (vllm-project/vllm#50729).\n"
          "        num_dst_tokens = conv_width - token_bias\n"
          "        for token_idx in range(0, num_dst_tokens):\n"
          "            for row_base in range(0, dim_rows, COPY_BLOCK_SIZE):\n"
          "                rows = row_base + offsets\n"
          "                mask = rows < dim_rows\n"
          "                src_byte_addr = (\n"
          "                    src_block_addr\n"
          "                    + rows * row_stride\n"
          "                    + (token_idx + token_bias) * state_elem_size\n"
          "                )\n"
          "                dst_byte_addr = (\n"
          "                    dst_addr + rows * row_stride + token_idx * state_elem_size\n"
          "                )\n"
          "                if state_elem_size == 2:\n"
          "                    src_u16 = src_byte_addr.to(tl.pointer_type(tl.uint16))\n"
          "                    dst_u16 = dst_byte_addr.to(tl.pointer_type(tl.uint16))\n"
          "                    data_u16 = tl.load(src_u16, mask=mask)\n"
          "                    tl.store(dst_u16, data_u16, mask=mask)\n"
          "                elif state_elem_size == 4:\n"
          "                    src_u32 = src_byte_addr.to(tl.pointer_type(tl.uint32))\n"
          "                    dst_u32 = dst_byte_addr.to(tl.pointer_type(tl.uint32))\n"
          "                    data_u32 = tl.load(src_u32, mask=mask)\n"
          "                    tl.store(dst_u32, data_u32, mask=mask)\n"
          "                else:\n"
          "                    for byte_idx in range(0, state_elem_size):\n"
          "                        src_u8 = (src_byte_addr + byte_idx).to(\n"
          "                            tl.pointer_type(tl.uint8)\n"
          "                        )\n"
          "                        dst_u8 = (dst_byte_addr + byte_idx).to(\n"
          "                            tl.pointer_type(tl.uint8)\n"
          "                        )\n"
          "                        data_u8 = tl.load(src_u8, mask=mask)\n"
          "                        tl.store(dst_u8, data_u8, mask=mask)\n"
      )
      count1 = src.count(anchor1)
      assert count1 == 1, f"DS-conv-dim-first anchor found {count1} times, expected 1 -- re-derive the patch"
      src = src.replace(anchor1, replacement1, 1)

      # Hunk 2: SD-conv branch -- add the same-block-overlap guard, falling
      # back to a token-by-token memmove-safe copy only for the genuinely
      # hazardous case (same block, real shift).
      anchor2 = (
          "        # Small per-block bytes (~60-80 KiB) make tiling degenerate, so\n"
          "        # conv runs as a single-CTA memcpy (NUM_TILES=1).\n"
          "        src_block_id = tl.load(block_table_base + src_col).to(tl.int64)\n"
          "        src_offset = token_bias.to(tl.int64) * state_inner_size * state_elem_size\n"
          "        src_addr = state_base_addr + src_block_id * state_block_stride + src_offset\n"
          "        copy_size = (\n"
          "            (conv_width - token_bias).to(tl.int64) * state_inner_size * state_elem_size\n"
          "        )\n"
          "        _memcpy_u64_tiled(\n"
          "            src_addr,\n"
          "            dst_addr,\n"
          "            copy_size,\n"
          "            tile_idx,\n"
          "            COPY_BLOCK_SIZE=COPY_BLOCK_SIZE,\n"
          "            NUM_TILES=1,\n"
          "        )\n"
      )
      replacement2 = (
          "        src_block_id = tl.load(block_table_base + src_col).to(tl.int64)\n"
          "        src_block_addr = state_base_addr + src_block_id * state_block_stride\n"
          "        token_bytes = state_inner_size * state_elem_size\n"
          "        num_dst_tokens = conv_width - token_bias\n"
          "\n"
          "        # [glm53-mamba-race-fix] Distinct blocks and exact self-copies\n"
          "        # cannot have a destructive overlap, so retain the\n"
          "        # u64-vectorized single-CTA copy -- see recipe header\n"
          "        # (vllm-project/vllm#50729).\n"
          "        if src_block_id != dest_block_id or token_bias == 0:\n"
          "            src_addr = src_block_addr + token_bias.to(tl.int64) * token_bytes\n"
          "            copy_size = num_dst_tokens.to(tl.int64) * token_bytes\n"
          "            _memcpy_u64_tiled(\n"
          "                src_addr,\n"
          "                dst_addr,\n"
          "                copy_size,\n"
          "                tile_idx,\n"
          "                COPY_BLOCK_SIZE=COPY_BLOCK_SIZE,\n"
          "                NUM_TILES=1,\n"
          "            )\n"
          "            return\n"
          "\n"
          "        # Copy tokens from low to high. Each token-sized source and\n"
          "        # destination region is disjoint, so same-block left shifts are\n"
          "        # memmove-safe without a barrier.\n"
          "        for token_idx in range(0, num_dst_tokens):\n"
          "            src_token = src_block_addr + (token_idx + token_bias) * token_bytes\n"
          "            dst_token = dst_addr + token_idx * token_bytes\n"
          "            _memcpy_u64_tiled(\n"
          "                src_token,\n"
          "                dst_token,\n"
          "                token_bytes,\n"
          "                tile_idx,\n"
          "                COPY_BLOCK_SIZE=COPY_BLOCK_SIZE,\n"
          "                NUM_TILES=1,\n"
          "            )\n"
      )
      count2 = src.count(anchor2)
      assert count2 == 1, f"SD-conv anchor found {count2} times, expected 1 -- re-derive the patch"
      src = src.replace(anchor2, replacement2, 1)

      kernel_copy._unsafe_update_src(src)

      # Hunk 3: batch_memcpy_kernel -- separate kernel, same hazard class.
      kernel_batch = _mamba_utils_mod.batch_memcpy_kernel
      src3 = kernel_batch.src

      anchor3a = (
          "    size = tl.load(sizes + pid)\n"
          "\n"
          "    offsets = tl.arange(0, BLOCK_SIZE)\n"
      )
      replacement3a = (
          "    size = tl.load(sizes + pid)\n"
          "    is_left_overlap = dst_ptr < src_ptr and dst_ptr + size > src_ptr\n"
          "\n"
          "    offsets = tl.arange(0, BLOCK_SIZE)\n"
      )
      count3a = src3.count(anchor3a)
      assert count3a == 1, f"batch_memcpy size/offsets anchor found {count3a} times, expected 1 -- re-derive the patch"
      src3 = src3.replace(anchor3a, replacement3a, 1)

      anchor3b = (
          "        data = tl.load(curr_src_ptr, mask=mask)\n"
          "        tl.store(curr_dst_ptr, data, mask=mask)\n"
      )
      replacement3b = (
          "        data = tl.load(curr_src_ptr, mask=mask)\n"
          "        if is_left_overlap:\n"
          "            # [glm53-mamba-race-fix] Preserve each lane's source before a\n"
          "            # lower-address lane stores over it -- see recipe header\n"
          "            # (vllm-project/vllm#50729). Condition is uniform within the\n"
          "            # program.\n"
          "            tl.debug_barrier()\n"
          "        tl.store(curr_dst_ptr, data, mask=mask)\n"
      )
      count3b = src3.count(anchor3b)
      assert count3b == 1, f"batch_memcpy store anchor found {count3b} times, expected 1 -- re-derive the patch"
      src3 = src3.replace(anchor3b, replacement3b, 1)

      kernel_batch._unsafe_update_src(src3)

      print(
          f"[glm53-mamba-race-fix] patched _copy_mamba_state_block (DS-conv "
          f"token-major loop, SD-conv same-block guard) and batch_memcpy_kernel "
          f"(left-overlap barrier) (pid={os.getpid()}, __name__={__name__!r})",
          file=sys.stderr, flush=True,
      )


  _patch_mamba_state_copy_race()


  def _fix_glm53_mixed_prefill_env():
      """GLM53_MIXED_PREFILL_CHUNK does not survive into the EngineCore
      subprocess under --distributed-executor-backend mp: confirmed via
      /proc/<pid>/environ that this var (plus GLM53_INDEXER_WORKSPACE and
      GLM53_SUPPRESS_STOPS_IN_REASONING) is present in this entrypoint
      process but absent from the spawned EngineCore's actual environment,
      while GLM53_FP8_LMHEAD_SCALE_MODE/GLM53_SPINWAIT_MS survive the same
      spawn. Root cause not fully pinned down (ruled out: the mp-backend's
      CoreEngineProcManager spawns via a bare context.Process() with no
      explicit env= filtering; Ray's get_env_vars_to_copy allowlist isn't
      in this backend's path at all) -- so sidestep it instead of chasing
      it further: this module reliably re-executes at import time in every
      spawned process (see the mamba-race-fix note above), so capture the
      value into a file on the FIRST execution (this entrypoint process
      always has it correct) and have every re-execution -- including a
      child whose own os.environ is missing the key -- read the file as a
      fallback. Monkeypatches the scheduler's policy function directly
      rather than re-deriving cap/skip logic at the call site.
      """
      import vllm.v1.core.sched.scheduler as _sched_mod

      state_path = "/tmp/glm53_mixed_prefill_chunk.state"
      if "GLM53_MIXED_PREFILL_CHUNK" in os.environ:
          with open(state_path, "w") as f:
              f.write(os.environ["GLM53_MIXED_PREFILL_CHUNK"])

      def _read_value():
          if "GLM53_MIXED_PREFILL_CHUNK" in os.environ:
              return os.environ["GLM53_MIXED_PREFILL_CHUNK"]
          try:
              with open(state_path) as f:
                  return f.read().strip()
          except OSError:
              return "skip"

      def _patched_policy(running, current):
          raw = _read_value().strip().lower()
          if raw in ("0", "off", "no"):
              return None
          if raw in ("skip", "-1"):
              cap = 0
          else:
              try:
                  cap = int(raw)
              except ValueError:
                  cap = 0
              if cap <= 0:
                  return None
          cur_id = getattr(current, "request_id", None)
          for r in running:
              if r is current or getattr(r, "request_id", None) == cur_id:
                  continue
              if r.num_computed_tokens >= r.num_prompt_tokens:
                  return cap
          return None

      _sched_mod._glm53_mixed_prefill_policy = _patched_policy
      print(
          f"[glm53-mixed-prefill-env-fix] value={_read_value()!r} pinned "
          f"into scheduler via {state_path} "
          f"(pid={os.getpid()}, __name__={__name__!r})",
          file=sys.stderr, flush=True,
      )


  _fix_glm53_mixed_prefill_env()

  if __name__ == "__main__":
      sys.argv[0] = "vllm"
      from vllm.entrypoints.cli.main import main
      sys.exit(main())
  PYSHIM
  python3 /tmp/glm53_v8_shim.py serve {model_path} \
      --served-model-name {served_model_name} \
      --host {host} --port {port} \
      --trust-remote-code \
      --tensor-parallel-size {tensor_parallel} \
      --quantization {quantization} \
      --gpu-memory-utilization {gpu_memory_utilization} \
      --max-model-len {max_model_len} \
      --max-num-seqs {max_num_seqs} \
      --kv-cache-dtype {kv_cache_dtype} \
      --max-num-batched-tokens {max_num_batched_tokens} \
      --speculative-config '{speculative_config}' \
      --tool-call-parser glm47 --enable-auto-tool-choice \
      --reasoning-parser glm45 \
      --enable-prefix-caching \
      --no-enable-flashinfer-autotune \
      --skip-mm-profiling \
      --limit-mm-per-prompt '{limit_mm}' \
      --chat-template /opt/glm53/chat_template.jinja \
      --default-chat-template-kwargs '{default_chat_template_kwargs}' \
      --distributed-executor-backend mp
